Folder Arrangement

AudioDose/
│
├── train.py                ← Trains the YOLOv8 model on your pill images
├── app.py                  ← Runs the web server and detection API
├── requirements.txt        ← List of Python packages to install
├── yolov8s.pt              ← pt file
│
├── models/
│   └── best.pt             ← Your trained model is saved here after training
│
├── templates/
│   └── index.html          ← The web UI (the page you see in the browser)
│
└── utils/
    └── pill_database.json  ← Database of pill info (name, dosage, warnings, etc.)

Download Anaconda Distribution
https://www.anaconda.com/download/success

Run it and Download anaconda_prompt 

Change the directory to the folder

cd Downloads\AudioDose

Run this command

conda activate yolov8_env

Run this command

python app.py

Go to this link made by using FastAPI
==================================================
  AudioDose — FastAPI Server
  http://localhost:8000
==================================================

do note that the HTML file is in the template folder and called index.html
learn and mess with the index.html's relationship with the backend
strictly index.html ang ginamit kong file name para mag port
